<div id="login" class="module">
    {% if _target_path is defined %}
        {{ render_hinclude(controller('FOSUserBundle:Security:loginTarget', { '_target_path': _target_path }), {'default': 'Loading...'}) }}
    {% else %}
        {{ render(controller('FOSUserBundle:Security:login'), {'default': 'Loading...'}) }}
    {% endif %}
</div>
{#
<div id="login">
    {% if error %}
        <div>{{ error.message }}</div>
    {% endif %}

    <form id="loginform" action="{{ path('login_check') }}" method="post">
        <label for="username">Usuario</label>
        <input type="text" id="username" name="_username" value="{{ last_username }}" />
        <br /><br /><br />
        <label for="password">Contraseña</label>
        <input type="password" id="password" name="_password" />
        <br /><br />
        <!--
            If you want to control the URL the user
            is redirected to on success (more details below)
            <input type="hidden" name="_target_path" value="/account" />
        -->

        <button type="submit">Acceder</button>
    </form>
</div>
#}
