<?xml version="1.0" encoding="UTF-8"?>
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <sitemap>
        <loc>{{ url('edemy_sitemap') }}</loc>
    </sitemap>
    {% for prefix in prefixes %}
        <sitemap>
            <loc>{{ url(prefix.value ~ '.edemy_sitemap') }}</loc>
        </sitemap>
    {% endfor %}
</sitemapindex>